export const NODE_ENV = process.env.NODE_ENV || 'development';

export const DEBUG = process.env.DEBUG || 'fatal';

export const DEBUG_LEVEL = process.env.DEBUG_LEVEL || 'error';

export const DEBUG_MONGO = process.env.DEbUG_MONGO || false;

export const PORT = process.env.PORT || 3000;

export const MONGO_URI = process.env.MONGO_URI || undefined;
